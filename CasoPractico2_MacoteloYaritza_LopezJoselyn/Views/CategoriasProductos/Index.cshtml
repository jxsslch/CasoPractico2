@model IEnumerable<CasoPractico2_MacoteloYaritza_LopezJoselyn.Models.CategoriasProducto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NombreCategoria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NombreCategoria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NombreCategoria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
